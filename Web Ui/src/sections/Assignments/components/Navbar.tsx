import { AppBar,Toolbar, Grid, Typography } from '@mui/material';

const Navbar = () => {
  return (
   <AppBar>
        <Toolbar>
            <Typography>
                TDDLab
            </Typography>
        </Toolbar>
   </AppBar>   
  )
}

export default Navbar
/*describe('Assignment Manager Interface Tests', () => {
    beforeEach(() => {
      cy.visit('/');
    });
  
    it('should display Assignments and Forms containers', () => {
      cy.get('[data-testid=assignments-container]').should('exist');
      cy.get('[data-testid=forms-container]').should('exist');
    });
  
    it('should show Assignment Form when the button is clicked', () => {
      cy.get('[data-testid=show-form-button]').click();
      cy.get('[data-testid=form-container]').should('exist');
    });
  
    it('should display Invitation Component', () => {
      cy.get('[data-testid=invitation-component]').should('exist');
    });
  });
  */